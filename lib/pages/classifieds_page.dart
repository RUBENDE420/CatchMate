import 'package:flutter/material.dart';

class ClassifiedsPage extends StatelessWidget {
  const ClassifiedsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Kleinanzeigen")),
    );
  }
}